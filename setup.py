from setuptools import setup, find_packages

setup(
    name="experiments",
    version="0.1.0",
    description="Experiments library",
    author="Kshitij Aggarwal",
    author_email="",
    packages=find_packages(),
    install_requires=[
        # List any dependencies required by your library
    ],
)
